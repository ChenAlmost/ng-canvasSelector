import { DrawCanvasDirective } from './draw-canvas.directive';

describe('DrawCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
